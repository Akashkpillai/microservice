version: '3.8'

services:
  reservations:
    build:
      context: .
      dockerfile: ./apps/reservations/Dockerfile
      target: development
    command: npm run start:dev reservations
    ports:
      - '3001:3000'
    volumes:
      - .:/usr/src/app

#   mongo:
#     image: mongo:latest
#     container_name: mongo
#     ports:
#       - '27017:27017'
#     volumes:
#       - mongo_data:/data/db

# volumes:
#   mongo_data:
